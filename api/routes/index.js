export async function get() {
    // Will also want a .json() method
    // Will also want availability to db
    // Will also want to be able to set headers
    // Will also want to be able to set status
    return new Response('get /');
}
